@model IEnumerable<GamesGlobalAssessment.Models.ViewEpisodes>

@{
    ViewData["Title"] = "Index";
}

<form style="background: url('@Url.Content("~/css/images/tv3.jpg")');">
    <div class="row md-Padding-Top-100 align-center">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <h1 class="display-4">Episode List</h1>
        </div>
    </div>
    <div class="row md-Padding-Top-10 align-center">
    </div>

    @using (Html.BeginForm("Index", "Episode", FormMethod.Post))
    {
        <div class="well white">
            <div class="row md-Padding-Bottom-15 md-Padding-left-15">
                <div class="col-md-2 md-Padding-left-15">
                    @Html.TextBox("search", null, new { @class = "form-control", @placeholder = "Search by Episode Name, Title etc..." })
                </div>
                <div class="col-md-1">
                    <input type="submit" value="Search" class="btn btn-block btn-info" />
                </div>
                <div class="col-md-1 col-xs-2 align-right">
                    <a asp-action="Create" data-toggle="tooltip" title="Add an Episode">
                        <i class="fa fa-2x fa-plus"></i>
                    </a>
                </div>
            </div>
        </div>
    }

    <table class="table table-bordered table-striped table-responsive">
        <thead>
            <tr>
                <th></th>
                <th>
                    @Html.DisplayName("Episode No")
                </th>
                <th>
                    @Html.DisplayName("Episode Name")
                </th>
                <th>
                    @Html.DisplayName("Season")
                </th>
                <th>
                    @Html.DisplayName("Title")
                </th>
                <th>
                    @Html.DisplayName("Watched")
                </th>
                <th>
                    @Html.DisplayName("Created By")
                </th>
                <th>
                    @Html.DisplayName("Year")
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.EpisodeID" data-toggle="tooltip" title="Details">
                            <i class="fa fa-2x fa-edit"></i>
                        </a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EpisodeNo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EpisodeName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Season)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Watched)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreatedBy)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Year)
                    </td>
                    <td>
                        <a asp-action="Delete" asp-route-id="@item.EpisodeID" data-toggle="tooltip" title="Delete">
                            <i class="fa fa-2x fa-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</form>